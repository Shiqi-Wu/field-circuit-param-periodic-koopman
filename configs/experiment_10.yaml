save_dir: '/home/shiqi/code/Project2-sensor-case/field-circuit-param-periodic-koopman/results/experiment_10'
data_dir: '/home/shiqi/code/Project2-sensor-case/field-circuit-param-periodic-koopman/data/differentMur'
step_size: 30
sample_step: 1
pca_dim: 4
batch_size: 256
validation_split: 0.2
dictionary_dim: 45
dictionary_layers: [2, 2, 2]
A_layers: [2, 2, 2]
B_layers: [2, 2, 2]
lr: 0.001
step_size_lr: 200
gamma_lr: 0.9
epochs: 5000
description: We will use the same data as in the first experiment. It is a unstructued koopman model as A(\nu)\Psi(x) + B(\nu)\Psi(x). In the prediction process, no projection is used. The step size is 30.